# 快速启动指南

## 🚀 三步启动项目

### 第一步：安装 Node.js（如果还没有）

1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本
3. 验证安装：
   ```cmd
   node --version
   npm --version
   ```

### 第二步：安装 pnpm

```cmd
npm install -g pnpm
```

验证安装：
```cmd
pnpm --version
```

### 第三步：安装依赖并启动

```cmd
# 进入项目目录
cd D:\project\practise\qingchun_travel_loan_package\qingchun_travel_loan

# 安装依赖（首次需要，约5-10分钟）
# 这会自动安装 cross-env 等所有必需的依赖包
pnpm install

# 启动前端开发服务器
pnpm dev:client
```

**注意**：`pnpm install` 会自动安装所有依赖，包括 `cross-env`（用于 Windows 系统兼容性）。如果遇到 `cross-env` 未找到的错误，可以手动安装：
```cmd
pnpm add -D cross-env
```

### 访问应用

浏览器打开：**http://localhost:3000**

---

## 📝 常用命令

| 命令 | 说明 |
|------|------|
| `pnpm dev:client` | 启动前端开发服务器 |
| `pnpm dev` | 启动全栈项目（前端+后端） |
| `pnpm install` | 安装项目依赖 |
| `pnpm build` | 构建生产版本 |

---

## ⚠️ 常见问题

### 问题：`'NODE_ENV' 不是内部或外部命令`

**原因**：Windows 系统不支持 Unix 风格的环境变量设置，需要 `cross-env` 包

**解决**：
```cmd
# 安装 cross-env（如果 pnpm install 后仍未安装）
pnpm add -D cross-env

# 然后重新运行
pnpm dev:client
```

### 问题：`Cannot find module 'vite'`

**解决**：
```cmd
rmdir /s /q node_modules
del pnpm-lock.yaml
pnpm install
```

### 问题：端口被占用

**解决**：修改 `vite.config.ts` 中的端口，或关闭占用端口的程序

---

## 📚 详细文档

查看 [启动指南-完整版.md](./启动指南-完整版.md) 获取详细说明

